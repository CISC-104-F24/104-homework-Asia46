using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      Debug.Log("Hello, world!"); 
      Debug.LogWarning("Hello, I am a warning!");
      Debug.LogError("ERROR! ERROR!");
      // Author: Asia 
      /* A game devlopment idea I have is a isometic cat cafe mobile game. 
       *  I don't have much figured out for it but I want it to be a two story cafe layout
       * where the main controls is tapping. 
       */
      
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
